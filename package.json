{
  "name": "@yamadayuki/parse5",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "build:watch": "lerna run --parallel build:watch",
    "test": "lerna run --parallel test",
    "test:watch": "lerna exec --parallel -- npm run test -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.8.0"
  }
}
